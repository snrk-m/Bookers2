<% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this obj from being saved:</h2>

      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
<% end %>


<div class="container px-5 px-sm-0">
<div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
       <h2>Editing Book</h2>
       <%= form_with model:@book, url:book_path(@book), local:true do |f| %>
         　<label for="inputName">Title</label>
    　 <%= f.text_field :title, class: "form-control name"%>
    　　     <label for="inputName">Opinion</label>
       <%= f.text_field :body, class: "form-control name"%>

         <br><%= f.submit nil, class: 'btn btn-primary' %>
         <% end %>

     <%= link_to "Show", book_path(@book.id), class: 'show_edit_link'%>
     |
     <%= link_to "Back",books_path %>

